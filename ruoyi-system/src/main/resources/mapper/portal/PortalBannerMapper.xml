<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.PortalBannerMapper">

    <resultMap id="PortalBannerResult" type="PortalBanner">
        <result property="bannerId" column="banner_id"/>
        <result property="bannerTitle" column="banner_title"/>
        <result property="bannerSubtitle" column="banner_subtitle"/>
        <result property="imageUrl" column="image_url"/>
        <result property="targetUrl" column="target_url"/>
        <result property="buttonText" column="button_text"/>
        <result property="sortNum" column="sort_num"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectPortalBannerVo">
        select banner_id, banner_title, banner_subtitle, image_url, target_url, button_text,
               sort_num, status, create_by, create_time, update_by, update_time, remark
        from portal_banner
    </sql>

    <select id="selectBannerById" parameterType="Long" resultMap="PortalBannerResult">
        <include refid="selectPortalBannerVo"/>
        where banner_id = #{bannerId}
    </select>

    <select id="selectBannerList" parameterType="PortalBanner" resultMap="PortalBannerResult">
        <include refid="selectPortalBannerVo"/>
        <where>
            <if test="bannerTitle != null and bannerTitle != ''">
                and banner_title like concat('%', #{bannerTitle}, '%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
        order by sort_num asc, banner_id desc
    </select>

    <select id="selectEnabledBannerList" resultMap="PortalBannerResult">
        <include refid="selectPortalBannerVo"/>
        where status = '0'
        order by sort_num asc, banner_id desc
    </select>

    <insert id="insertBanner" parameterType="PortalBanner" useGeneratedKeys="true" keyProperty="bannerId">
        insert into portal_banner (
            banner_title, banner_subtitle, image_url, target_url, button_text,
            sort_num, status, create_by, create_time, remark
        ) values (
            #{bannerTitle}, #{bannerSubtitle}, #{imageUrl}, #{targetUrl}, #{buttonText},
            #{sortNum}, #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateBanner" parameterType="PortalBanner">
        update portal_banner
        <set>
            banner_title = #{bannerTitle},
            banner_subtitle = #{bannerSubtitle},
            image_url = #{imageUrl},
            target_url = #{targetUrl},
            button_text = #{buttonText},
            sort_num = #{sortNum},
            status = #{status},
            remark = #{remark},
            update_by = #{updateBy},
            update_time = sysdate()
        </set>
        where banner_id = #{bannerId}
    </update>

    <delete id="deleteBannerByIds" parameterType="Long">
        delete from portal_banner where banner_id in
        <foreach item="bannerId" collection="array" open="(" separator="," close=")">
            #{bannerId}
        </foreach>
    </delete>

</mapper>
