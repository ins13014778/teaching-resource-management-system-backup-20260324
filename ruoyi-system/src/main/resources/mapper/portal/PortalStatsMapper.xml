<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.PortalStatsMapper">

    <select id="countCourses" resultType="int">
        select count(*) from edu_course
    </select>

    <select id="countResources" resultType="int">
        select count(*)
        from edu_resource
        where del_flag = '0'
    </select>

    <select id="countTeachers" resultType="int">
        select count(distinct ur.user_id)
        from sys_user_role ur
        inner join sys_user u on u.user_id = ur.user_id
        where ur.role_id = 3
          and u.del_flag = '0'
          and u.status = '0'
    </select>

    <select id="countStudents" resultType="int">
        select count(distinct ur.user_id)
        from sys_user_role ur
        inner join sys_user u on u.user_id = ur.user_id
        where ur.role_id = 4
          and u.del_flag = '0'
          and u.status = '0'
    </select>

</mapper>
